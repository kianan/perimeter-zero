{
  "status": "building",
  "brief": "brief-pause-bug",
  "increments": [
    {
      "title": "Add Player paused state and gate fire()",
      "description": "In Player.ts, add a new `private paused = false` field (separate from the existing `frozen` field) and two public methods, `pause()` and `resume()`, that set/clear it. Update fire()'s early-return guard to include the new flag.",
      "acceptance": [
        "Player.ts declares a `private paused = false` field distinct from `frozen`",
        "Player.ts exposes `pause(): void` that sets `this.paused = true` and `resume(): void` that sets `this.paused = false`",
        "fire()'s early-return condition reads `if (this.dead || this.frozen || this.paused) return;`",
        "move() is unchanged (no reference to the new `paused` field)",
        "`tsc --noEmit` passes"
      ]
    },
    {
      "title": "Wire Player pause/resume into GameScene's augment-choice pause",
      "description": "In GameScene.ts, call `this.player.pause()` inside openAugmentChoice() alongside the existing `this.physics.pause()`, and call `this.player.resume()` inside resumeAfterAugmentChoice() alongside the existing `this.physics.resume()`.",
      "acceptance": [
        "openAugmentChoice() (GameScene.ts:387) calls `this.player.pause()` in addition to `this.physics.pause()`",
        "resumeAfterAugmentChoice() (GameScene.ts:417) calls `this.player.resume()` in addition to `this.physics.resume()`",
        "No other lines in GameScene.ts changed; Bullet.ts and AoeLob.ts are untouched",
        "`tsc --noEmit` passes"
      ]
    },
    {
      "title": "Verify fix against original repro and full regression suite",
      "description": "Manually/agent-drive the original repro via window.__qaGame (main.ts's dev hook): open the Augment Choice popup, wait ~4s with no input, and confirm no new bullets are created while paused === true. Also confirm bullets fired before the popup opened freeze in place during the pause and resume from their pre-pause position (not a stale-position burst). Then run adversarial_qa/qa_agent.py's full check suite to confirm the other 8 checks (boundary clamping, idle-stuck, edge-fire, double-click, restart cycling) still pass.",
      "acceptance": [
        "With the Augment Choice popup open for ~4s and no input, `window.__qaGame` bullet count/list shows zero new bullets created while `player.paused === true`",
        "A bullet fired immediately before opening the popup shows no position change during the pause window, then resumes moving from its pre-pause position after resume (no teleport/burst)",
        "adversarial_qa/qa_agent.py's boundary-clamping check passes",
        "adversarial_qa/qa_agent.py's idle-stuck check passes",
        "adversarial_qa/qa_agent.py's edge-fire check passes",
        "adversarial_qa/qa_agent.py's double-click check passes",
        "adversarial_qa/qa_agent.py's restart-cycling check passes",
        "`tsc --noEmit` passes with no errors"
      ]
    }
  ],
  "current": 6
}